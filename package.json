{
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  },
  "scripts": {
    "build": "node build.js && node scripts/copy-static.js && cp manifest.json dist/manifest.json && node scripts/fix-manifest.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
